graph.get(node);
for (int neighbor : graph.get(node)) {
     // process neighbor
      }

// explaination

graph.get(node) // This retrieves the list of neighbors (adjacent vertices) for the specified node in the graph. In an adjacency list representation of a graph, each node has a list that contains all the nodes it is directly connected to.

graph.get(0)   // [1, 2]
graph.get(1)   // [3]
graph.get(2)   // [3]
graph.get(3)   // []


for (int neighbor : graph.get(0)) {
    System.out.println(neighbor);
}//output:
1
2

