class Solution {

    int min = Integer.MAX_VALUE;
    Integer previous = null;

    public int getMinimumDifference(TreeNode root) {

        inorder(root);

        return min;
    }

    private void inorder(TreeNode node) {

        if (node == null) {
            return;
        }

        // Visit left subtree
        inorder(node.left);

        // Process current node
        if (previous != null) {
            min = Math.min(min, node.val - previous);
        }

        previous = node.val;

        // Visit right subtree
        inorder(node.right);
    }
}

//"Because this is a BST, I can use inorder traversal to visit the nodes in sorted order. 
// The minimum absolute difference must occur between two adjacent values in this sorted sequence.
//  Therefore, during inorder traversal I keep track of the previous node value and compare it with the current node. 
// I update the minimum difference at each step."

//Time: O(n) where n is the number of nodes in the BST
//Space: O(h) where h is the height of the BST due to recursion stack














//Let's Understand With Example
        4
       / \
      2   6
     / \
    1   3

Inorder traversal:

1 → 2 → 3 → 4 → 6
Step 1 — Node 1

We reach:

1

previous is:

null

So there is nothing to compare.

Then:

previous = node.val;

Therefore:

previous = 1
Step 2 — Node 2

Current:

node.val = 2

Previous:

previous = 1

Calculate:

node.val - previous
2 - 1 = 1

So:

min = 1

Then:

previous = 2
Step 3 — Node 3
current = 3
previous = 2

Difference:

3 - 2 = 1

Minimum remains:

min = 1
Step 4 — Node 4
current = 4
previous = 3

Difference:

4 - 3 = 1
Step 5 — Node 6
current = 6
previous = 4

Difference:

6 - 4 = 2

Final:

min = 1